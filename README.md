# Jarvis AI

Professional AI assistant project.

## Features
- Persistent memory (memory folder)
- Voice, image, code generation placeholders
- Search Wikipedia, Reddit, and internet (planned)
- TypeScript structured source (`src` folder)

## How to run
1. Install dependencies: `npm install`
2. Start project: `npm start`
